{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --prefix packages/core && npm install --prefix packages/database && npm install --prefix apps/dashboard",
  "buildCommand": "npm run build:dashboard",
  "outputDirectory": "apps/dashboard/.next"
}
